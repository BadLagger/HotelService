spring:
  application:
    name: booking-service

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
  instance:
    prefer-ip-address: false

management:
  endpoints:
    web:
      exposure:
        include: "*"